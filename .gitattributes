# Written by :tools:baselineprofile and committed so a build needs no device (docs/performance.md).
# Marked generated so GitHub collapses them in a pull request's diff and leaves them out of the
# language stats.
app/src/main/generated/baselineProfiles/** linguist-generated=true
